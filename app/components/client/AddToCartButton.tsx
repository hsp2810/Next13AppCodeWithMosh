"use client";

import React from "react";

const AddToCartButton = () => {
  return (
    <button onClick={() => window.alert("I have been clicked by Harshit")}>
      Click Me!
    </button>
  );
};

export default AddToCartButton;
